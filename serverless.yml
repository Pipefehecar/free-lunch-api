service: free-launch-api-monorepo

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  # profile: free-launch
  stage: dev

custom:
  webpack:
    concurrency: 3
  commonExcludes:
    - node_modules/**
# plugins:
#   - serverless-plugin-monorepo
plugins:
  - serverless-plugin-common-excludes
  - serverless-plugin-include-dependencies
  - serverless-plugin-monorepo
functions:
  api:
    handler: kitchen/src/lambda.handler
    events:
      - httpApi:
          path: /api/v1
          method: post
      - httpApi:
          path: /api/v1
          method: get

  # sqsListener:
  #   handler: src/lambda.sqsHandler
  #   events:
  #     - sqs:
  #         arn: ${cf:free-launch-api-monorepo-dev.ResponseQueueArn}
  #         batchSize: 1

  # Resources:
  #   ApiGatewayRestApi:
  #     Type: AWS::ApiGateway::RestApi
  #     Properties:
  #       Name: free-launch-api-gateway
  #   IngredientsRequestQueue:
  #     Type: AWS::SQS::Queue
  #     Properties:
  #       QueueName: warehouse-ingredients-request-queue
  #       VisibilityTimeout: 30

  #   IngredientsResponseQueue:
  #     Type: AWS::SQS::Queue
  #     Properties:
  #       QueueName: warehouse-ingredients-response-queue
  #       VisibilityTimeout: 30

  # Outputs:
  #   ApiGatewayRestApiId:
  #     Value: !Ref ApiGatewayRestApi
  #   ApiGatewayRootResourceId:
  #     Value: !GetAtt ApiGatewayRestApi.RootResourceId
  #   RequestQueueUrl:
  #     Value: !Ref IngredientsRequestQueue
  #   RequestQueueArn:
  #     Value: !GetAtt IngredientsRequestQueue.Arn
  #   ResponseQueueUrl:
  #     Value: !Ref IngredientsResponseQueue
  #   ResponseQueueArn:
  #     Value: !GetAtt IngredientsResponseQueue.Arn

resources:
  - ${file(aws_resources/gateway-response.yml)}
